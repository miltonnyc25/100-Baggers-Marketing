# 小红书 Slide Deck 多图发布 Skill

## 概述
将 NotebookLM 生成的 Slide Deck (PDF) 转换为图片，**所有页面合并为一篇多图笔记**发布到小红书。

## ⚠️ 重要规则

1. **封面图必须是 3:4 比例**（小红书推荐格式）
2. **所有 slides 合并为一篇笔记**（多图形式，最多 18 张）
3. **标题 ≤ 20 字符**
4. **发布时设为"公开可见"**
5. **遵循内容合规规则**（参考 xiaohongshu-publish Skill）

---

## 快速使用

### 自动发布脚本

```bash
# 发布所有 slides 为一篇多图笔记
python ~/clawd/skills/xiaohongshu-slides/xhs_slides_publisher.py \
    ~/clawd/newsletters/slides-2026-01-27-3x4 \
    2026-01-27

# 只发布前 9 张（如果 slides 太多）
python ~/clawd/skills/xiaohongshu-slides/xhs_slides_publisher.py \
    ~/clawd/newsletters/slides-2026-01-27-3x4 \
    2026-01-27 \
    --end 9
```

**脚本功能：**
- 自动加载已保存的 cookies（免登录）
- 一次性上传所有图片（最多 18 张）
- 自动填写标题和描述
- 自动点击发布

---

## 完整工作流程

### Step 1: 将 PDF 转换为图片

```bash
# 安装 poppler（如果没有）
brew install poppler

# PDF 转 PNG（每页一张）
pdftoppm -png -r 300 ~/clawd/newsletters/YYYY-MM-DD-slides.pdf ~/clawd/newsletters/slides/slide

# 输出文件：
# ~/clawd/newsletters/slides/slide-1.png
# ~/clawd/newsletters/slides/slide-2.png
# ...
```

### Step 2: 调整图片比例为 3:4

小红书图文推荐 3:4 比例（1080x1440 或 1242x1660）。

```bash
# 使用 ImageMagick 调整比例
for img in ~/clawd/newsletters/slides/slide-*.png; do
  convert "$img" \
    -gravity center \
    -background white \
    -extent 1080x1440 \
    "${img%.png}-3x4.png"
done
```

### Step 3: 发布到小红书

**方法 1: 使用自动脚本（推荐）**

```bash
python ~/clawd/skills/xiaohongshu-slides/xhs_slides_publisher.py \
    ~/clawd/newsletters/slides-2026-01-27-3x4 \
    2026-01-27
```

**方法 2: 手动发布**

```
1. 打开 https://creator.xiaohongshu.com/publish/publish
2. 点击 "上传图文"
3. 一次性选择所有 3:4 比例图片（最多 18 张）
4. 填写标题（≤20字符）
5. 填写描述（包含摘要 + 免责声明 + 话题标签）
6. 设置可见范围为"公开可见"
7. 点击发布
```

---

## 自动生成的标题和描述

**标题格式：**
```
美股日报MMDD｜N页完整版
```
例：`美股日报0127｜12页完整版`

**描述模板：**
```
📊 美股日报 YYYY-MM-DD 完整版

🔥 今日热点股票深度解析
📈 市场走势 + 个股分析

👉 向左滑动查看全部 N 页

#美股 #股票 #投资理财 #每日复盘 #美股日报

⚠️ 免责声明：所有观点均来自原始分析文章，不代表本账号立场，不构成投资建议。投资有风险，请谨慎决策。
```

---

## 文件命名规范

```
~/clawd/newsletters/
├── YYYY-MM-DD-slides.pdf              # 原始 Slide Deck PDF
├── slides-YYYY-MM-DD-3x4/             # 3:4 比例图片目录
│   ├── slide-1-3x4.png
│   ├── slide-2-3x4.png
│   ├── slide-3-3x4.png
│   └── ...
```

---

## 内容合规规则

**与 xiaohongshu-publish Skill 相同：**

1. **敏感话题过滤** - 台海、中美、制裁等必须删除
2. **观点归属** - 高估/低估必须标注"据分析"
3. **免责声明** - 描述末尾必须添加

---

## 脚本参数

```bash
python xhs_slides_publisher.py <slides_dir> <date> [--start N] [--end M]

参数:
  slides_dir    slides 图片目录（包含 *-3x4.png 文件）
  date          日期，格式 YYYY-MM-DD（用于生成标题）
  --start N     从第 N 张开始（默认: 1）
  --end M       到第 M 张结束（默认: 全部，最多 18）
```

---

## 依赖

```bash
# macOS
brew install poppler imagemagick

# Python
pip install playwright Pillow
playwright install chromium
```

---

## 工作流程清单

```
□ 1. 确认 Slide Deck PDF 已下载
□ 2. PDF 转图片（pdftoppm）
□ 3. 图片转 3:4 比例（1080x1440）
□ 4. 运行自动发布脚本
□ 5. 确认发布成功
```

---

## 限制

- 小红书单篇笔记最多 **18 张图片**
- 标题最长 **20 字符**
- 如果 slides 超过 18 张，建议分成两篇发布，或选择最重要的 18 张
